#include <stdio.h>

/**
 * main - a program that prints all single digit numbers
 * Return: Always 0 (success)
 */
int main(void)
{
int a;

for (a = 48; a < 58; a++)
{
putchar(a);
}

putchar('\n');
return (0);
}
